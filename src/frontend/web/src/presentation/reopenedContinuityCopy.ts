/** Product-wide en-dash “reopened” suffix (Phase 135) — Analyze ranked + Compare pair handoffs. */
export function withReopenedSuffix(leadingLabel: string): string {
  return `${leadingLabel} — reopened`
}
