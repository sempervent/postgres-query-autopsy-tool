# Copy to `.env.testing` or pass via `docker compose --env-file …`.
# PQAT_E2E_ENABLED=true  → registers /api/e2e/seed/* (testing only).

PQAT_E2E_ENABLED=true

# Future auth-mode E2E (not wired yet): e.g. bearer token or compose env for Auth:Enabled
# PQAT_AUTH_E2E=0
