## Sort rewrite: Sort → Incremental Sort

**Intent**
- Stress sort-family rewrite where the operator name changes but purpose remains.
- Validate mapping does not overclaim high confidence if context is weak.
- Validate new sort-specific finding rules (Phase 8) fire with evidence (sort key, time share).

