## Materialization effect: Materialize added under repeated loops

**Intent**
- Stress repeated-subtree / loops amplification behavior.
- Validate mapping when `Materialize` appears/disappears in the same zone.
- Validate new Materialize+loops rule (Phase 8) triggers with evidence.

